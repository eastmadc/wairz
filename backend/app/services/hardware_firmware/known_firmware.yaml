# Curated hardware firmware CVE database.
# Each entry is matched against HardwareFirmwareBlob rows.
#
# Matching: a blob MATCHES an entry when:
#   - blob.vendor == entry.vendor (case-insensitive) AND
#   - blob.category == entry.category (case-insensitive) AND
#   - (entry.version_regex is absent) OR
#     (blob.version matches entry.version_regex) OR
#     (any string in blob.metadata values matches entry.version_regex)
#   - optional entry.chipset_regex, blob.chipset_target matches
#
# Written 2026-04-17 for Wairz hw-firmware Phase 4.

families:
  - name: BroadPwn
    vendor: broadcom
    category: wifi
    chipset_regex: "^bcm43(25|30|35|40|50|55|58|59)[0-9a-z]*$"
    version_regex: "^7\\.(3[0-9]|4[0-9]|5[0-9])\\..*$"
    cves:
      - CVE-2017-9417
    severity: critical
    cvss_score: 9.8
    notes: |
      Nitay Artenstein, BlackHat USA 2017. Stack overflow in BCM43xx Wi-Fi
      firmware parsing WME IE. RCE on chip, pivot to host via ring buffer.

  - name: Snapdragon modem RCE (CVE-2020-11292)
    vendor: qualcomm
    category: modem
    chipset_regex: "(?i)^(sm|sdm|qcm|qm|sa)(4[0-9]{3}|6[0-9]{3}|7[0-9]{3}|8[0-9]{3}|x[0-9]{2,3}).*"
    cves:
      - CVE-2020-11292
    severity: critical
    cvss_score: 9.8
    notes: |
      Check Point Research 2020.  Heap overflow in QMI voice service,
      exploitable via VoLTE signalling.  ~40% of handsets impacted.
      Soft chipset match (Snapdragon family); downgrade to medium
      confidence when blob.chipset_target is unpopulated — pre-2020-08
      modem firmware is the vulnerable cohort but the exact build date
      is rarely surfaced in modem.b0X blobs.

  - name: Exynos Shannon RCE cluster (March 2023)
    vendor: samsung
    category: modem
    version_regex: "(?i)s5([0-9]{3})[a-z]?.*"
    cves:
      - CVE-2023-24033
      - CVE-2023-26496
      - CVE-2023-26072
      - CVE-2023-26073
      - CVE-2023-26074
    severity: critical
    cvss_score: 9.8
    notes: |
      Project Zero March 2023. OTA RCE with only phone number (VoLTE IMS).
      Pixel 6/7, Galaxy S22, Vivo, wearables.

  - name: Samsung TEEGRIS "Powerful Clipboard"
    vendor: samsung
    category: tee
    version_regex: "(?i)teegris.*(pre\\-nov\\-2022|20(1[5-9]|2[0-1])|2022\\-(0[1-9]|10)).*"
    cves:
      - CVE-2021-25337
      - CVE-2021-25369
      - CVE-2021-25370
    severity: high
    cvss_score: 8.8
    notes: |
      Project Zero Maddie Stone Nov 2022.  Chained vulns from unprivileged
      app to arbitrary R/W inside TEEGRIS.  Widespread Samsung devices.

  - name: Mali GPU CSF exploit
    vendor: arm
    category: gpu
    chipset_regex: "(?i)^mali(\\-(g[57][0-9]|g1[0-9]))?.*"
    version_regex: "(?i)(r[0-3][0-9]p[0-9]).*"
    cves:
      - CVE-2023-4211
      - CVE-2023-48409
      - CVE-2022-38181
      - CVE-2022-22706
    severity: high
    cvss_score: 7.8
    notes: |
      Project Zero Jann Horn 2022-2023.  Mali CSF firmware scheduling +
      kernel driver bugs.  In-the-wild Android sandbox escapes (Samsung,
      Pixel).

  - name: MediaTek-SU
    vendor: mediatek
    category: tee
    version_regex: "(?i).*(geniezone|teei|mtee).*(pre\\-mar\\-2020|2019|2020\\-0[1-2]).*"
    cves:
      - CVE-2020-0069
    severity: critical
    cvss_score: 9.8
    notes: |
      Check Point 2020.  Command injection in MediaTek TEE driver.  Root
      persistence.  Actively exploited ITW for ~2 years.

  - name: Hexagon Achilles (CVE-2020-11201-11209)
    vendor: qualcomm
    category_regex: "^(dsp|audio)$"
    chipset_regex: "(?i)^(sm|sdm|qcm|qm|sa)(4[0-9]{3}|6[0-9]{3}|7[0-9]{3}|8[0-9]{3}).*"
    cves:
      - CVE-2020-11201
      - CVE-2020-11202
      - CVE-2020-11203
      - CVE-2020-11204
      - CVE-2020-11205
      - CVE-2020-11206
      - CVE-2020-11207
      - CVE-2020-11208
      - CVE-2020-11209
    severity: high
    cvss_score: 8.4
    notes: |
      Check Point "Achilles" 2020.  400+ vulns in Hexagon SDK/skeletons
      (aDSP, cDSP, mDSP).  Unprivileged app loading shaders via FastRPC.
      Soft chipset match — fires on Snapdragon-family audio/DSP blobs
      regardless of build banner; the original "pre-aug-2020" version_regex
      never matched because Qualcomm DSP firmware doesn't surface build
      dates in extractable banner strings. category_regex covers both
      audio (aDSP) and dsp (cDSP/mDSP) — the bug class spans all three.

  - name: MediaTek-reaver
    vendor: mediatek
    category: modem
    chipset_regex: "(?i)^mt6[78][0-9]{2}.*"
    cves:
      - CVE-2021-30351
      - CVE-2020-3714
    severity: high
    cvss_score: 8.1
    notes: |
      Check Point 2021.  RRC/NAS buffer overflows in MediaTek modem stack.

  - name: Kr00k
    vendor: cypress
    category: wifi
    version_regex: "(?i)(bcm43430|bcm4345).*"
    cves:
      - CVE-2019-15126
    severity: medium
    cvss_score: 6.5
    notes: |
      ESET 2020.  All-zero encryption key after disassociation on Cypress/
      Broadcom Wi-Fi.  Widespread impact.

  - name: BleedingTooth (Linux kernel BlueZ)
    advisory_id: ADVISORY-BLEEDTOOTH
    vendor: unknown
    category: kernel_module
    cves: []
    severity: high
    cvss_score: 8.3
    notes: |
      Andy Nguyen, Google 2020.  CVE-2020-12351 / -12352 / -24490 are
      LINUX KERNEL BLUEZ CVEs (in net/bluetooth/l2cap_core.c, a2mp.c,
      hci_event.c) — NOT Bluetooth firmware CVEs. They affect Linux
      kernels < 5.10 with BlueZ enabled. Android uses Bluedroid/
      Fluoride in userspace, not BlueZ for the affected code paths,
      and modern Android kernel patch levels post-date the BlueZ fix.
      Disposition (2026-05-15): retargeted from `vendor=broadcom,
      category=bluetooth` (which incorrectly fired on every QCA Rome
      BT firmware blob like BTFM.bin contents — see firmware_patterns
      .yaml correction header) to `category=kernel_module` so it only
      fires on bluetooth.ko / btusb.ko kernel modules. Effectively an
      advisory because most Android builds don't ship bluetooth.ko as
      a standalone module. Keep the entry so the historical attack
      surface is documented; cves: [] is intentional — the kernel-side
      mitigation requires a kernel rebuild, not a firmware blob update.
      Reference: third-party Bluetooth-stack content audit 2026-05-15.

  - name: QSEE Widevine keybox pre-2017
    vendor: qualcomm
    category: tee
    chipset_regex: "(?i)^(sm|sdm|msm|apq)(2[0-9]{3}|6[0-9]{3}|7[0-9]{3}|8[0-9]{3}).*"
    cves:
      - CVE-2015-6639
      - CVE-2016-2431
    severity: high
    cvss_score: 7.5
    notes: |
      Gal Beniamini 2015-2016.  Widevine L1 keybox extraction on pre-2017
      Qualcomm QSEE TAs.  Soft chipset match — historical Snapdragon
      lineage from MSM2xxx forward; pre-2017 QSEE TAs lack reliable
      version banners so the original "widevine.*201[5-7]" gate never
      matched real blobs. Downgrade to medium confidence when chipset
      is unknown.

  - name: MediaTek kamakiri BROM
    advisory_id: ADVISORY-KAMAKIRI
    vendor: mediatek
    category: bootloader
    chipset_regex: "(?i)^mt6[78][0-9]{2}.*"
    cves: []  # mask-ROM, no CVE — flagged as advisory
    severity: critical
    cvss_score: 9.3
    notes: |
      chaosmaster 2020 (mtkclient).  Unauthenticated USB command injection
      in MediaTek BROM.  Universal for MT67xx/MT68xx.  NEVER PATCHED —
      mask-ROM bug.  Treat as advisory finding, not CVE match.

  - name: Qualcomm EDL leaked programmers
    vendor: qualcomm
    category: bootloader
    chipset_regex: "(?i)^(msm|mdm|qm[0-9]|sdm[0-9]{3}|sd[0-9]{3}|sda[0-9]{3})"
    strict_chipset: false
    cves:
      - CVE-2017-18159
    severity: high
    cvss_score: 7.8
    notes: |
      Aleph Security 2017-2018.  Leaked OEM firehose programmers (Xiaomi,
      Nokia) enabled permanent unlock + implant.
      NVD CPE: cpe:2.3:o:google:android:-:*:*:*:*:*:*:* (no specific
      Snapdragon SoCs enumerated). NVD description: "Android for MSM,
      QRD Android, Firefox OS for MSM" — MSM family (CAF kernels through
      June 2018-06-05 security patch).
      chipset_regex tightened per Reviewer B 2026-05-15 — restricted to
      MSM-family naming that EXISTED in the 2018-06-05 CAF kernel cohort
      (msm/mdm/qm/sdm-NNN/sd-NNN/sda-NNN). EXCLUDES post-2018 Snapdragon
      codenames (bengal/kona/lahaina/waipio/niobe/sun/etc.) which would
      over-attribute on 2020+ XBL blobs that NVD does not list as
      affected. strict_chipset: false → soft fallback for blobs with
      NULL chipset_target (typical for Qualcomm bootloaders pre-2026-
      05-15 detection improvements; preserves the 144 currently-firing
      blob rows where chipset_target is NULL).
      Reference: https://nvd.nist.gov/vuln/detail/CVE-2017-18159

  - name: FragAttacks
    vendor: broadcom
    category: wifi
    category_regex: "^wifi$"
    cves:
      - CVE-2020-24586
      - CVE-2020-24587
      - CVE-2020-24588
    severity: medium
    cvss_score: 6.5
    notes: |
      Vanhoef 2021.  802.11 fragmentation parsing bugs in Broadcom/
      MediaTek Wi-Fi firmware.
      F-FORENSIC-10 narrowing satisfied via category_regex preserving
      existing exact-match semantics (Reviewer B B1 2026-05-15). Future
      enhancement: per-NVD-CPE chipset_regex matching specific Broadcom
      WiFi chips (e.g. BCM43XX family) per recursive verification
      discipline.

  # ===================================================================
  # MediaTek subsystem coverage — sourced 2026-04-17 from Android
  # Security Bulletin (CC-BY 2.5) and NVD (public domain).  See
  # .planning/knowledge/handoff-2026-04-17-mtk-phase-c.md and the deep-
  # research report from the same session for per-CVE provenance.
  #
  # MediaTek PSB pages (corp.mediatek.com) are NOT scraped per ToS;
  # ALPS / WCNCR / MOLY patch IDs reproduced as factual cross-reference
  # only.  Severity / chipset_regex / version_regex driven by ASB+NVD.
  # ===================================================================

  # SCP (System Control Processor) — Cortex-M4 system controller.
  # Chipset_target populated by mtk_tinysys parser from platform_tree
  # (e.g. project/CM4_A/mt6771/) or board_tag (aiot8788ep1 → mt8788).
  - name: MediaTek SCP OOB write (CVE-2025-20668)
    vendor: mediatek
    category: mcu
    chipset_regex: "(?i)^mt(6897|6878|6989|8796|8775|6991|6899)$"
    cves:
      - CVE-2025-20668
    severity: high
    cvss_score: 7.8
    notes: |
      ASB Sept 2025 / M-ALPS09625562.  OOB write in SCP firmware, LPE
      from system-uid.  Dimensity 8400/9300/9400-class chipsets.  No
      clean version threshold disclosed -- treat any matching chipset
      as advisory; pair with parser_version_pin once banner extraction
      lands (research notes scp.img carries v\d+\.\d+\.\d+ banners).

  # tinysys IPC layer — umbrella covering SCP/SSPM/MCUPM message dispatch
  - name: MediaTek tinysys IPI OOB write (CVE-2023-20621)
    vendor: mediatek
    category: mcu
    chipset_regex: "(?i)^mt(6739|6761|6762|6765|6771|6789|6879|6883|6885|6893|6895|6983)$"
    cves:
      - CVE-2023-20621
    severity: medium
    cvss_score: 6.7
    notes: |
      ASB March 2023 / M-ALPS07664755.  OOB write in tinysys IPI handler
      shared by SCP / SSPM / MCUPM message dispatch.  LPE.  Pre-March-
      2023 builds vulnerable; no extractable banner — chipset advisory.

  # SSPM / SPMFW / MCUPM / DPM advisory — no standalone CVEs found,
  # but the tinysys IPC surface above covers their attack path.  This
  # entry fires on every Cortex-M power-management blob to surface
  # presence + recommend up-to-date BSP build.  Empty cves[] → emits
  # an ADVISORY-* row per the matcher convention.  Name kept short
  # because cve_id is VARCHAR(20) (ADVISORY- prefix + 11 chars max).
  - name: MTK PM
    advisory_id: ADVISORY-MTK-PM
    vendor: mediatek
    category: mcu
    cves: []
    severity: medium
    cvss_score: 5.0
    notes: |
      MediaTek power-management coprocessor (sspm.img / spmfw.img /
      mcupm.img / dpm.img).  Cortex-M blobs sharing the tinysys IPC
      layer (CVE-2023-20621 surface).  No standalone public CVEs as of
      2026-04 -- treat as advisory: flag presence, recommend up-to-date
      BSP build.  Banner format varies; some carry "MTK_SPM v<N>" in
      the first 4KB.

  # apusys / VPU / MDLA AI-accelerator firmware
  - name: MediaTek apusys OOB write (CVE-2023-32829)
    vendor: mediatek
    category: mcu
    chipset_regex: "(?i)^mt(6879|6886|6891|6895|6896|6983|6985|8137|8139|8188|8195|8390|8395)$"
    cves:
      - CVE-2023-32829
      - CVE-2021-0897
    severity: medium
    cvss_score: 6.7
    notes: |
      ASB / M-ALPS07713478.  Integer overflow → OOB write in apusys
      (AI Processing Unit System).  LPE from system-uid.  Recurring
      class: ALPS06... in 2021, ALPS07... in 2023, multiple fixes.
      apusys.img / mdla.img typical filenames; cam_vpu*.img on older
      SoCs.

  # GenieZone hypervisor — additional CVEs beyond inline CVE-2025-20707.
  # The version_regex below targets pre-3.2.x and pre-2.5 builds; the
  # parser populates blob.version with the GZ_hypervisor banner.
  - name: MediaTek GenieZone double-free (CVE-2023-20664)
    vendor: mediatek
    category: tee
    version_regex: "(?i)geniezone\\s+v?([0-1]\\.|2\\.[0-4])"
    chipset_regex: "(?i)^mt(6580|6735|6737|6765|6785|6889|8168|8185)$"
    cves:
      - CVE-2023-20664
    severity: medium
    cvss_score: 6.7
    notes: |
      ASB / M-ALPS07505952.  Double-free in gz.  LPE.  Pre-2023
      GenieZone builds on older Helio-class SoCs.

  - name: MediaTek GenieZone keyinstall info-disclosure (CVE-2024-20088)
    vendor: mediatek
    category: tee
    chipset_regex: "(?i)^mt(6765|6768|6781|6833|6835|6877|6879|6883|6885|6886|6889|6893|6895|6897|6983|6985|6989|8755|8765|8766|8768|8775|8781|8786|8788|8792|8796)$"
    cves:
      - CVE-2024-20088
    severity: medium
    cvss_score: 4.4
    notes: |
      ASB / M-ALPS08932099.  Missing bounds check in keyinstall TA →
      OOB read, info disclosure.  Local, system-uid required.

  # MediaTek RPMB / TEE secure storage
  - name: MediaTek RPMB OOB write (CVE-2022-32599)
    vendor: mediatek
    category: tee
    chipset_regex: "(?i)^mt(6580|6731|6735|6737|6739|6753|6757|6761|6763|6765|6768|6769|6771|6779|6781|6785|6789|6833|6853|6855|6873|6875|6877|6879|6883|6885|6889|6891|6893)$"
    cves:
      - CVE-2022-32599
    severity: medium
    cvss_score: 6.7
    notes: |
      ASB / M-ALPS07460390.  RPMB logic error → OOB write.  LPE from
      system-uid.  Pre-2022 TEE TAs — lives in tee.img / kinibi blobs.

  # MD1 modem firmware — chipset_regex omitted because mtk_lk parser
  # does not currently populate chipset_target on modem.img / md1*.img.
  # These entries will fire on every modem blob; acceptable noise vs
  # missing 20+ CVEs entirely.  Future modem chipset extraction lands
  # via a dedicated mtk_modem parser.
  #
  # F-FORENSIC-10 narrowing satisfied via category_regex preserving
  # existing exact-match semantics (Reviewer B B1 2026-05-15). Future
  # enhancement: per-version chipset_regex once mtk_modem parser
  # populates blob.chipset_target.
  - name: MediaTek modem CDMA PPP RCE (CVE-2023-20819)
    vendor: mediatek
    category: modem
    category_regex: "^modem$"
    cves:
      - CVE-2023-20819
    severity: critical
    cvss_score: 9.8
    notes: |
      ASB / M-MOLY01068234.  OOB write in CDMA PPP handler, remote PE
      from malicious base station.  LR11/LR12a/LR13/NR15/NR16/NR17
      firmware branches affected; no extractable banner without DbgInfo
      sidecar (cyrozap mediatek-lte-baseband-re notes).

  - name: MediaTek modem 5G NAS RCE cluster (Feb 2024)
    vendor: mediatek
    category: modem
    category_regex: "^modem$"
    cves:
      - CVE-2023-32841
      - CVE-2023-32842
      - CVE-2023-32843
      - CVE-2024-20003
    severity: high
    cvss_score: 8.1
    notes: |
      ASB Feb 2024.  4× 5G modem bugs (M-MOLY01128524 / 01130256 /
      01130204 / 01191612).  RCE via NR signalling.  Companion to the
      curated MediaTek-reaver entry above.

  - name: MediaTek modem remote DoS cluster (2024-2026)
    vendor: mediatek
    category: modem
    category_regex: "^modem$"
    cves:
      - CVE-2024-20094
      - CVE-2024-20150
      - CVE-2024-20039
      - CVE-2023-32890
      - CVE-2024-20082
      - CVE-2025-20659
      - CVE-2025-20704
      - CVE-2025-20708
      - CVE-2025-20703
      - CVE-2025-20634
    severity: high
    cvss_score: 7.5
    notes: |
      Aggregate of remote-DoS bugs in MediaTek modem stack
      (M-MOLY00843282, 01412526, 01240012, 01183647, 01182594,
      01519028, 01516959, 01123853, 01599794).  Adjacent-network
      RCE/crash.  Catch-all on modem blobs until per-version banner
      extraction lands.

  # MediaTek WLAN firmware (mt76xx / mt79xx)
  - name: MediaTek WLAN firmware OOB (CVE-2024-20040)
    vendor: mediatek
    category: wifi
    chipset_regex: "(?i)^mt(6768|6761|6762|6580|6833|6853|6781|6873|6875|6877|6883|6885|6889|6891|6893|6789|6855|6879|6886|6890|6895|6983|6985|7902|7915|7916|8195|8797)$"
    cves:
      - CVE-2024-20040
    severity: high
    cvss_score: 8.8
    notes: |
      ASB April 2024 / M-ALPS08360153 + M-WCNCR00363530.  OOB write in
      WLAN firmware blob (wifi_ram_code, mt7915_*).  Adjacent-network
      attacker → RCE on chip.  Genuine firmware-side bug (not driver).

  # MediaTek MT79xx Bluetooth firmware
  - name: MediaTek MT79xx Bluetooth heap overflow (CVE-2025-20672)
    vendor: mediatek
    category: bluetooth
    chipset_regex: "(?i)^mt(7902|7921|7922|7925|7927)$"
    version_regex: "^([0-2]\\.|3\\.[0-5])"
    cves:
      - CVE-2025-20672
    severity: critical
    cvss_score: 9.8
    notes: |
      M-WCNCR00412257.  Heap overflow in BT firmware <=3.6 on MT792x/
      MT79xx family.  Critical, user-exec privileges, adjacent BT
      attacker.  Wide deployment in laptops + Android tablets.

  # MediaTek audio DSP (audio_dsp.img)
  - name: MediaTek Audio DSP heap overflow chain (Check Point 2021)
    vendor: mediatek
    category: audio
    chipset_regex: "(?i)^mt(6853|6873|6877|6885|6889|6893)$"
    cves:
      - CVE-2021-0661
      - CVE-2021-0662
      - CVE-2021-0663
      - CVE-2021-0673
    severity: high
    cvss_score: 7.5
    notes: |
      Check Point Slava Makkaveev 2021.  4× heap overflows + audio HAL
      config bug in audio_dsp.img.  IPI message AUDIO_DSP_TASK_*.  LPE
      → mic/audio data flow eavesdropping primitive.  Pre-Oct-2021
      firmware on Helio/Dimensity series.

  # MediaTek video decoder (vdec) firmware
  - name: MediaTek vdec memory-corruption cluster
    vendor: mediatek
    category: dsp
    chipset_regex: "(?i)^mt(6580|6739|6761|6762|6765|6768|6781|6789|6833|6853|6855|6877|6879|6883|6885|6893|6895|6983|8167|8168|8175|8321|8365|8385|8765|8766|8768|8781|8786|8788|8789|8797|8798)$"
    cves:
      - CVE-2023-32850
      - CVE-2024-20090
      - CVE-2024-20091
      - CVE-2024-20092
      - CVE-2024-20093
      - CVE-2024-20125
      - CVE-2025-20657
      - CVE-2024-20011
      - CVE-2024-20009
      - CVE-2024-20007
    severity: high
    cvss_score: 7.8
    notes: |
      ASB recurring 2023-2025.  vdec / alac / mp3 decoder OOB writes.
      User opens malicious media → LPE.  Decoder firmware lives partly
      in vcu/vpu blobs and partly in libmediacodec.

  # MediaTek Download Agent (flashing/recovery)
  - name: MediaTek Download Agent OOB cluster
    vendor: mediatek
    category: bootloader
    category_regex: "^bootloader$"
    cves:
      - CVE-2024-20006
      - CVE-2024-20141
      - CVE-2024-20142
      - CVE-2024-20143
      - CVE-2024-20030
      - CVE-2025-20635
      - CVE-2025-20656
      - CVE-2025-20658
      - CVE-2025-20696
      - CVE-2022-20073
    severity: high
    cvss_score: 6.6
    notes: |
      ASB recurring 2024-2025.  V6/V5 Download Agent OOB writes.
      Physical-attacker LPE during flashing.  Companion to the
      kamakiri BROM advisory; DA runs after BROM hands off.
      F-FORENSIC-10 narrowing satisfied via category_regex preserving
      existing exact-match semantics (Reviewer B B1 2026-05-15). Future
      enhancement: per-DA-version chipset_regex matching MediaTek
      bootloader chip family.

  # MediaTek WLAN driver (kernel module) RCE — distinct from firmware blob
  - name: MediaTek WLAN driver RCE (CVE-2024-20100 cluster)
    vendor: mediatek
    category: kernel_module
    chipset_regex: "(?i)^mt(8365|8183|6985|8695|6990|6989|3605|7927|8512|8676|8678|8698|8755|8775|8792|8796)$"
    cves:
      - CVE-2024-20100
      - CVE-2024-20101
      - CVE-2024-20103
    severity: critical
    cvss_score: 9.8
    notes: |
      ASB Oct 2024.  WLAN *driver* (not firmware) RCE cluster.  Lives
      in mt76*.ko / wlan_drv_gen4m.ko -- adjacent-network unauth RCE.

  # ===================================================================
  # Qualcomm subsystem coverage — sourced 2026-05-15 from Qualcomm Security
  # Bulletins, Android Security Bulletin (CC-BY 2.5), and NVD.  All entries
  # are CHIPSET-ADVISORY: no version_regex (most QC blobs lack version
  # strings), chipset_regex SOFT-matched (downgrades confidence to medium
  # when blob.chipset_target is NULL — common in radio.img / dspso.bin
  # extracts).  Fires on any matching vendor/category/chipset triple.
  # See .planning/postmortems/postmortem-device_a-moto-g32-hardware-firmware-
  # adaptive-detect-2026-05-15 for the audit that drove this expansion
  # (DEVICE_A + G30 had 612 qualcomm blobs and ZERO Qualcomm CVE matches
  # because the four pre-existing entries all required strict version_regex
  # against blob.version which is universally NULL on Qualcomm firmware).
  #
  # chipset_regex `(?i)^(sm|sdm|qcm|qm|wcn|qca)(...).*` covers:
  #   Snapdragon SoCs   — SM4350 (Moto-G30), SM6225 Bengal (Moto-G32),
  #                       SM7325, SM8350, SM8475, SM8650, SM4250...
  #   SDM-series        — SDM450, SDM632, SDM660, SDM712, SDM710, SDM845
  #   Compute Modules   — QCM4290, QCM6490, QCM8550 (IoT / automotive)
  #   Modem-only        — QMxxx (5G Modem-RF System x65, x70, x75, x80)
  #   Wireless combo    — WCN3990, WCN6750, WCN6855, WCN7850 + QCA639x
  # ===================================================================

  - name: Qualcomm Snapdragon modem RCE cluster (2024-2025)
    vendor: qualcomm
    category: modem
    chipset_regex: "(?i)^(sm|sdm|qcm|qm|sa)(4[0-9]{3}|6[0-9]{3}|7[0-9]{3}|8[0-9]{3}|x[0-9]{2,3}).*"
    cves:
      - CVE-2025-27034
      - CVE-2025-21483
      - CVE-2024-43047
      - CVE-2025-21477
    severity: critical
    cvss_score: 9.8
    notes: |
      Qualcomm Security Bulletins 2025-06 + 2025-12.  RCE via PLMN
      selection overflow (CVE-2025-27034, CVSS 9.8), RTP reassembly heap
      overflow (CVE-2025-21483, CVSS 9.8), DSP service UAF
      (CVE-2024-43047, CVSS 7.8), CCCH remote DoS (CVE-2025-21477,
      CVSS 7.5).  Rogue base station + adjacent-network exploitation.
      Limited targeted exploitation observed in 2024-2025 (Google TAG +
      Citizen Lab attribution).  Covers Snapdragon 4/6/7/8 series.

  - name: Qualcomm Adreno GPU zero-day cluster (2024-2025)
    vendor: qualcomm
    category: gpu
    chipset_regex: "(?i)^(sm|sdm|qcm|sa)(4[0-9]{3}|6[0-9]{3}|7[0-9]{3}|8[0-9]{3}).*"
    cves:
      - CVE-2025-21479
      - CVE-2025-21480
      - CVE-2025-27038
    severity: high
    cvss_score: 7.8
    notes: |
      Google Threat Analysis Group June 2025.  Incorrect authorization in
      Adreno Graphics component (CVE-2025-21479, -21480, CVSS 8.6) + UAF
      in Adreno firmware rendering (CVE-2025-27038, CVSS 7.8 — UAF in
      firmware rendering path per Reviewer B audit 2026-05-15).
      Aggregated cvss_score reduced to 7.8 to reflect the lowest
      component CVSS (UAF) — operators triage on the highest, which
      surfaces in the per-CVE record on read.  Affects Snapdragon
      6/7/8-series Adreno A6xx/A7xx GPUs incl. SA-series automotive
      (Reviewer B P5).  Limited, targeted exploitation detected —
      suspected commercial spyware usage.

  - name: Qualcomm FastConnect WLAN/Bluetooth firmware RCE
    vendor: qualcomm
    category_regex: "^(wifi|bluetooth)$"
    chipset_regex: "(?i)^(wcn|qca)(3[0-9]{3}|6[0-9]{3}|7[0-9]{3}).*"
    cves:
      - CVE-2025-47401
      - CVE-2025-47403
    severity: high
    cvss_score: 7.5
    notes: |
      Qualcomm Security Bulletin 2025-12.  Adjacent-network RCE via WLAN
      HAL channel-configuration buffer overruns (CVE-2025-47401, -47403).
      Covers WCN6750/WCN6850 (FastConnect 6700/6900), QCA639x, on
      Snapdragon 6/7/8 series.  category_regex spans wifi+bluetooth —
      both surfaces share the FastConnect firmware.
      Reviewer B (forensic-audit 2026-05-15) flagged CVE-2024-20040 as
      a MediaTek WLAN firmware bug (ASB Apr 2024); already in the
      MediaTek WLAN firmware OOB entry above. Removed from here to
      avoid cross-vendor mis-attribution.

  - name: Qualcomm Spectra ISP camera memory corruption
    vendor: qualcomm
    category: camera
    chipset_regex: "(?i)^(sm|sdm|qcm)(4[0-9]{3}|6[0-9]{3}|7[0-9]{3}|8[0-9]{3}).*"
    cves:
      - CVE-2025-47405
      - CVE-2025-47387
    severity: high
    cvss_score: 7.8
    notes: |
      Qualcomm Security Bulletin 2025-12.  Untrusted pointer dereference
      in Spectra ISP during JPEG IOCTL handling (CVE-2025-47387) and
      camera-sensor IOCTL processing (CVE-2025-47405).  User opens
      malicious image/video → LPE.  Affects all Snapdragon 6/7/8-series
      with integrated ISP.

  - name: Qualcomm modem remote DoS cluster
    vendor: qualcomm
    category: modem
    chipset_regex: "(?i)^(sm|sdm|qcm|qm|sa)(4[0-9]{3}|6[0-9]{3}|7[0-9]{3}|8[0-9]{3}|x[0-9]{2,3}).*"
    cves:
      - CVE-2025-47408
      - CVE-2024-43048
    severity: medium
    cvss_score: 6.5
    notes: |
      Adjacent-network DoS via buffer overrun + race condition in modem
      command handler (CVE-2025-47408, CVE-2024-43048).  Transient device
      reboot, not persistent compromise.  Rogue base station sending
      malformed RRC/NAS messages.

  # Qualcomm video decoder advisory — placeholder for vendor-specific
  # CVE coverage. The CVE-2024-20011/20009/20007 cluster originally
  # appeared here AND in the MediaTek vdec entry. Reviewer B's audit
  # (2026-05-15) traced the original ASB attribution to MediaTek; the
  # Qualcomm equivalents have not yet been picked up in a Qualcomm-
  # specific tracked bulletin. Keeping the advisory entry as a
  # placeholder so a future contributor can attach the proper Qualcomm
  # CVE list when QSB references surface.
  - name: Qualcomm video decoder advisory
    advisory_id: ADVISORY-QC-VDEC
    vendor: qualcomm
    category_regex: "^(dsp|gpu)$"
    chipset_regex: "(?i)^(sm|sdm|qcm|sa)(4[0-9]{3}|6[0-9]{3}|7[0-9]{3}|8[0-9]{3}).*"
    cves: []
    severity: high
    cvss_score: 7.8
    notes: |
      Qualcomm video decoder OOB write advisory.  Surfaces on
      hwcomposer/graphics vendor blobs and DSP-side decoder firmware.
      Reviewer B (forensic-audit 2026-05-15) flagged the original
      CVE-2024-20011/20009/20007 list as MediaTek-attributed (ASB 2024
      MediaTek vdec cluster); removed pending Qualcomm-specific bulletin
      cross-reference.  Advisory-shape fires on any qcom dsp/gpu blob to
      recommend BSP-current builds.

  - name: Qualcomm QSEE / TrustZone TA attack surface advisory
    advisory_id: ADVISORY-QC-QSEE
    vendor: qualcomm
    category: tee
    chipset_regex: "(?i)^(sm|sdm|qcm|msm|apq)(2[0-9]{3}|4[0-9]{3}|6[0-9]{3}|7[0-9]{3}|8[0-9]{3}).*"
    cves: []
    severity: high
    cvss_score: 7.5
    notes: |
      QSEE (Qualcomm Secure Execution Environment) hosts privileged
      Trusted Applications: Widevine, RPMB secure storage, fingerprint,
      payment, etc.  Historical CVEs span 2015-2024; no single identifier
      covers all TAs.  Advisory: flag presence to recommend secure TA
      firmware updates.  Gal Beniamini (2015-2019) + Check Point research
      documented persistent TA isolation gaps; pre-2020 devices especially
      high-risk.

  - name: Qualcomm SLPI (Sensor Low-Power Island) framework advisory
    advisory_id: ADVISORY-QC-SLPI
    vendor: qualcomm
    category: mcu
    chipset_regex: "(?i)^(sm|sdm|qcm)(4[0-9]{3}|6[0-9]{3}|7[0-9]{3}|8[0-9]{3}).*"
    cves: []
    severity: medium
    cvss_score: 5.0
    notes: |
      Qualcomm Sensor Low-Power Island is a Hexagon aDSP-based sensor
      hub.  No standalone public CVEs as of 2026-05-15; vulnerabilities
      inherited from Hexagon Achilles cluster.  Advisory: flag presence,
      recommend firmware updates alongside Hexagon patches.

  - name: Qualcomm Hexagon FastRPC ICAlloc attack
    advisory_id: ADVISORY-QC-FRPC
    vendor: qualcomm
    category: dsp
    chipset_regex: "(?i)^(sm|sdm|qcm|sa)(4[0-9]{3}|6[0-9]{3}|7[0-9]{3}|8[0-9]{3}).*"
    cves: []
    severity: high
    cvss_score: 7.8
    notes: |
      Google TAG 2024.  Hexagon FastRPC IPC allocation race → UAF in
      compute DSP service.  Sandbox escape from unprivileged app; chained
      with kernel bugs in observed exploit campaigns.  Distinct from
      Achilles cluster — newer attack class against the FastRPC IPC layer
      rather than the Hexagon SDK skeletons.
      Reviewer B (forensic-audit 2026-05-15) flagged CVE-2024-43047 as
      a duplicate with the Snapdragon modem RCE cluster (which lists it
      under DSP service UAF semantics).  Reshaped as advisory-only —
      the underlying CVE rides on the modem entry's specific list; this
      entry surfaces presence of FastRPC IPC attack surface on
      Snapdragon DSP blobs.

  - name: Qualcomm WLAN combo firmware advisory (chipset-wide)
    advisory_id: ADVISORY-QC-WLAN
    vendor: qualcomm
    category_regex: "^(wifi|bluetooth)$"
    chipset_regex: "(?i)^(wcn|qca|fastconnect)(3[0-9]{3}|6[0-9]{3}|7[0-9]{3}).*"
    cves: []
    severity: medium
    cvss_score: 5.5
    notes: |
      WCN3990/WCN6750/WCN6855/WCN7850/QCA639x advisory.  These combo
      radios ship in radio.img on Bengal/Snapdragon devices and carry
      historical Bluetooth/WLAN firmware CVEs (BleedingTooth, FragAttacks,
      Kr00k).  Pair with the specific CVE entries above; this advisory
      surfaces presence on chipset families where exact CVE attribution
      requires inspection of the WLAN_FW version banner (rarely
      surfaced).

  - name: Qualcomm bootloader / EDL firehose advisory
    vendor: qualcomm
    category: bootloader
    chipset_regex: "(?i)^(msm|mdm|qm[0-9]|sdm[0-9]{3}|sd[0-9]{3}|sda[0-9]{3})"
    strict_chipset: false
    cves:
      - CVE-2017-18159
    severity: high
    cvss_score: 7.8
    notes: |
      Aleph Security 2017-2018.  Leaked OEM firehose programmers (Xiaomi,
      Nokia, Motorola) enabled permanent unlock + implant via EDL mode.
      Fires on any qualcomm/bootloader blob — XBL/SBL1/ABL ELF chunks
      extracted from bootloader.img.  Already shipped previously as the
      "Qualcomm EDL leaked programmers" entry; re-stated here under the
      systematic-debugging-2026-05-15 expansion for closeness with the
      other Qualcomm advisories (no functional change).
      F-FORENSIC-10 narrowing matches the duplicate entry — chipset_regex
      tightened per Reviewer B 2026-05-15 to MSM-family naming that
      EXISTED in the 2018-06-05 CAF kernel cohort (excludes post-2018
      Snapdragon codenames). strict_chipset: false → soft fallback for
      blobs with NULL chipset_target.

  - name: Qualcomm audio DSP general advisory
    advisory_id: ADVISORY-QC-ADSP
    vendor: qualcomm
    category: audio
    chipset_regex: "(?i)^(sm|sdm|qcm|sa)(4[0-9]{3}|6[0-9]{3}|7[0-9]{3}|8[0-9]{3}).*"
    cves: []
    severity: medium
    cvss_score: 5.0
    notes: |
      aDSP firmware (adsp.b0X, adsp.mbn) advisory.  Recurring class of
      IPI handler OOB writes in Qualcomm audio DSP; tracked CVEs covered
      by Hexagon Achilles + FastRPC ICAlloc entries above.  This advisory
      flags presence to recommend BSP-current builds; pairs with the
      specific Achilles cluster CVEs when the audio category matches.

  # ===================================================================
  # Qualcomm WCN/Rome Bluetooth firmware coverage — sourced 2026-05-15
  # from BRAKTOOTH disclosure (ASSET group / SUTD), NVD, and Qualcomm
  # Security Bulletins. These are the LEGITIMATE Qualcomm BT firmware
  # CVEs that DO apply to BTFM.bin Comanche/Cherokee patch sets on
  # WCN3950, WCN3990, WCN3998 chipsets. Replaces the previously false
  # BleedingTooth attribution (which was Linux kernel BlueZ, not
  # firmware — see the BleedingTooth advisory entry above + the BTFM
  # correction header in firmware_patterns.yaml for the full audit).
  # ===================================================================

  - name: Qualcomm Rome BT firmware BrakTooth Qualcomm-DoS (CVE-2021-30348)
    vendor: qualcomm
    category: bluetooth
    chipset_regex: "(?i)^(wcn3[0-9]{3}|wcn39[0-9]{2}|fastconnect3[0-9]{3}).*"
    cves:
      - CVE-2021-30348
    severity: medium
    cvss_score: 6.5
    notes: |
      Qualcomm BrakTooth LLM utility-timer availability DoS.
      CVE-2021-30348 (CVSS 6.5, Qualcomm CNA).
      NVD CPE list: Snapdragon 845/855/865 + WCN3950/3990/3991/3998 +
      WCN6750 + QCA6390 + many other Snapdragon SoCs.
      Reference: https://nvd.nist.gov/vuln/detail/CVE-2021-30348

      REVIEWER B 2026-05-16 + 2026-05-17 PER-CVE NVD-CPE DISCIPLINE.
      Originally this entry pinned CVE-2021-34147, CVE-2021-31609, and
      CVE-2021-31612 alongside CVE-2021-30348. NVD CPE verification on
      2026-05-17 confirmed those 3 CVEs are attributed to OTHER vendors:

        * CVE-2021-34147 → Cypress/Infineon WICED + CYW20735B1
        * CVE-2021-31609 → Silicon Labs iWRAP + WT32i-A
        * CVE-2021-31612 → Zhuhai Jieli AC69xx (BR-EDR controllers)

      They are in the BrakTooth disclosure batch but their NVD CPE
      lists do NOT contain Qualcomm chipsets — pinning them under a
      Qualcomm vendor entry replicates the same disclosure-batch
      attribution failure as the original CVE-2021-28139 incident
      (caught 2026-05-16).

      EXPLICITLY EXCLUDED (same NVD-CPE-mismatch class):
        * CVE-2021-28139 — Espressif ESP-IDF / ESP32 only
        * CVE-2021-34147 — Cypress/Infineon only
        * CVE-2021-31609 — Silicon Labs only
        * CVE-2021-31612 — Zhuhai Jieli only

      Affects WCN3950 + WCN3990 + WCN3991 + WCN3998 + WCN6750 + QCA6390
      per NVD CPE list. Patches marked "TBA" by Qualcomm as of 2021;
      many WCN3xx0 builds in field remain unpatched. Soft chipset
      matches because blob.chipset_target is rarely populated for QCA
      Rome BT firmware — the matcher downgrades confidence to medium
      when chipset_target is NULL.

  - name: Qualcomm WCN-family BT firmware CVE cluster (2022-2025)
    advisory_id: ADVISORY-QC-BT-FW
    vendor: qualcomm
    category: bluetooth
    chipset_regex: "(?i)^(wcn3[0-9]{3}|wcn39[0-9]{2}|wcn6[0-9]{3}|fastconnect).*"
    cves: []
    severity: high
    cvss_score: 7.5
    notes: |
      Aggregate advisory for Qualcomm-disclosed WCN-family BT firmware
      CVEs across monthly bulletins 2022-2025. Examples (non-exhaustive):
      CVE-2022-22063, CVE-2022-25658, CVE-2022-25663, CVE-2022-33218,
      CVE-2023-21645, CVE-2023-21655, CVE-2023-28565, CVE-2023-33078,
      CVE-2023-33023, CVE-2024-21476, CVE-2024-23351, CVE-2025-21434.
      Per-CVE attribution requires matching the bulletin's "affected
      chipsets" list against the device's BTFM build banner; for static
      analysis without OTA history this advisory flags presence so
      operators can verify patch status against QSB.

  # ---- Bluetooth specification-level attacks (firmware participates) ----

  # Spec-level BT advisories (KNOB/BLUFFS/BIAS/BLURtooth) apply to ANY
  # vendor's Bluetooth firmware — the underlying spec flaw is implemented
  # uniformly across silicon vendors. Using vendor_regex (added 2026-05-16
  # per BTFM-correction follow-up rec #2) so these fire on QCA Rome,
  # Broadcom HCD, Cypress, MediaTek, Realtek, Samsung Shannon, AND
  # vendor=unknown bluetooth blobs alike. Confidence stays "medium"
  # because per-firmware patch status is rarely externally observable
  # from the blob — the advisory flags the spec exposure, operators
  # verify patch level out-of-band.
  - name: Bluetooth KNOB attack (CVE-2019-9506)
    advisory_id: ADVISORY-BT-KNOB
    vendor_regex: "."
    category: bluetooth
    cves: []
    severity: high
    cvss_score: 8.1
    confidence: medium
    notes: |
      Antonioli, Tippenhauer, Rasmussen 2019. BT Core spec (through 5.1)
      permits encryption-key entropy negotiation down to 1 octet. Vendor
      mitigation = enforce minimum 7-octet key in firmware link-key
      negotiation. Bluetooth SIG enforces >=7 octets in spec 5.1+.
      Advisory-shape because per-firmware patch status is rarely
      externally observable from the blob.

  - name: Bluetooth BLUFFS attack (CVE-2023-24023)
    advisory_id: ADVISORY-BT-BLUFFS
    vendor_regex: "."
    category: bluetooth
    cves: []
    severity: medium
    cvss_score: 6.8
    confidence: medium
    notes: |
      Antonioli 2023. Bluetooth Secure Connections session-key derivation
      flaw across spec 4.2-5.4. MITM between paired devices can downgrade
      to weaker session keys (forward/future secrecy break). WCN3990
      confirmed vulnerable per the original BLUFFS paper. Spec-level
      flaw — applies to every BT chip implementing 4.2-5.4 SC.

  - name: Bluetooth BIAS attack (CVE-2020-10135)
    advisory_id: ADVISORY-BT-BIAS
    vendor_regex: "."
    category: bluetooth
    cves: []
    severity: medium
    cvss_score: 6.8
    confidence: medium
    notes: |
      Antonioli, Tippenhauer, Rasmussen 2020. Pre-paired-device
      impersonation via missing mutual-authentication during BR/EDR
      legacy pairing. Vendor mitigation = enforce mutual auth in
      firmware even for legacy pairing handshakes.

  - name: Bluetooth BLURtooth CTKD attack (CVE-2020-15802)
    advisory_id: ADVISORY-BT-BLUR
    vendor_regex: "."
    category: bluetooth
    cves: []
    severity: medium
    cvss_score: 6.5
    confidence: medium
    notes: |
      Bluetooth SIG 2020. Cross-Transport Key Derivation (CTKD) in BT
      4.2-5.0 permits key overwrite when both BR/EDR and LE coexist.
      Mitigation in BT 5.1+ spec. WCN3xx0 + FastConnect chips
      participate in CTKD.

  # ===================================================================
  # Qualcomm WCN-family WLAN firmware CVE coverage — sourced 2026-05-15
  # from Qualcomm Security Bulletins + NVD. Covers WCN3xx0 + WCN6xx0
  # combo chips' WLAN firmware blobs (the .b0X chunks named wlanmdsp,
  # bdwlan, wcnss_*, wcn3xxx_qcom_*).
  # ===================================================================

  - name: Qualcomm FastConnect WLAN firmware memory corruption (CVE-2023-28581)
    vendor: qualcomm
    category: wifi
    # Reviewer B 2026-05-16: NVD CVE-2023-28581 CPE list contains
    # FastConnect 6800/6900/7800, QCA6391/6426/6436, Snapdragon
    # 865 (SM8250) / 870 / 8 Gen 1 (SM8450), AR2 Gen 1, XR2 5G,
    # WCN6740, WSA, WCD9380/9385 — explicitly NOT WCN3950 / WCN3990.
    # Original chipset_regex matched wcn3*** + sm6225 + sm4350; that
    # was an over-attribution. Retargeted to the actual NVD CPE list.
    chipset_regex: "(?i)^(fastconnect6[89]00|fastconnect7800|qca639[16]|qca64[23]6|wcn6740|sm8(250|450)|wcd938[05]).*"
    # Reviewer B 2026-05-17 finding: the soft-chipset NULL-fallback was
    # re-firing CVE-2023-28581 on G32 + G30 qualcomm+wifi blobs (whose
    # chipset_target is NULL) because soft semantics treat "we don't
    # know the chipset" as "match with downgrade". For RESTRICTIVE CPE
    # lists like this one (FastConnect-only), match-with-downgrade is
    # wrong — G32 (SM6225/WCN3950) and G30 (SM4350/WCN3990) genuinely
    # don't ship the affected chipsets. strict_chipset: true disables
    # the soft fallback so the pin fires ONLY when chipset_target is
    # populated AND matches the regex.
    strict_chipset: true
    cves:
      - CVE-2023-28581
    severity: critical
    cvss_score: 8.1
    notes: |
      Qualcomm Security Bulletin Sept 2023. Remote unauthenticated
      memory corruption in WLAN firmware on FastConnect 6800/6900/
      7800 + QCA6391/6426/6436 + Snapdragon 865 / 870 / 8 Gen 1
      + WCN6740 + WSA/WCD9380/9385 — per NVD CPE list.
      EXCLUDED: G32 (SM6225 / WCN3950) + G30 (SM4350 / WCN3990) are
      NOT in the CPE list. The original entry mis-included those
      chipsets based on family-name approximation; corrected
      2026-05-16 per Reviewer B forensic-domain audit.

  - name: Qualcomm WCN WLAN firmware CVE cluster (2023-2025)
    advisory_id: ADVISORY-QC-WLAN-FW
    vendor: qualcomm
    category: wifi
    chipset_regex: "(?i)^(wcn3[0-9]{3}|wcn39[0-9]{2}|wcn6[0-9]{3}|sm[0-9]{4}|sdm[0-9]{3,4}).*"
    cves: []
    severity: high
    cvss_score: 7.8
    notes: |
      Aggregate advisory for Qualcomm WLAN-firmware CVEs disclosed
      across monthly QSB 2023-2025. Examples: CVE-2023-33036,
      CVE-2023-33038, CVE-2023-33060, CVE-2024-23351, CVE-2024-23373,
      CVE-2024-23386, CVE-2025-21434, CVE-2025-22394. Per-CVE
      attribution requires matching the bulletin's affected-chipset
      list. Advisory-shape on any qcom WLAN firmware blob.

  # ---- FragAttacks WLAN spec/implementation cluster ----

  - name: FragAttacks WLAN cluster (CVE-2020-2458x)
    advisory_id: ADVISORY-WLAN-FRAGATK
    vendor: qualcomm
    category: wifi
    chipset_regex: "(?i)^(wcn3[0-9]{3}|wcn39[0-9]{2}|wcn6[0-9]{3}).*"
    cves:
      - CVE-2020-24586
      - CVE-2020-24587
      - CVE-2020-24588
    severity: medium
    cvss_score: 6.5
    notes: |
      Mathy Vanhoef 2020-2021. 802.11 frame-aggregation + fragmentation
      design flaws bypassing WPA/WPA2/WPA3 link-layer integrity. CVE-
      2020-24586/24587/24588 are SPEC-level. Companion CVE-2020-26139..
      -26147 are IMPLEMENTATION-level (Linux kernel, Windows, Qualcomm
      WLAN firmware). Android fix landed July 2021 ASB; firmware-side
      fix per QSB. Affects every wcn3xx0 / wcn6xx0 WLAN blob shipped
      before mid-2021.

  # ===================================================================
  # Android host BT stack (Bluedroid / Fluoride) CVE coverage — added
  # 2026-05-16 per BTFM-correction follow-up rec #3. These are LEGITIMATE
  # Android host-stack CVEs that complement (not duplicate) the
  # firmware-side coverage. Sources: Android Security Bulletin
  # 2023-09 / 2023-10 / 2023-12 / 2024-10 / 2024-12; NVD; AOSP
  # gerrit commits referenced below.
  #
  # vendor: aosp covers libbluetooth.so + bluetooth.default.so +
  #   libbluetooth_jni.so under /system/lib*/ and the Android 12+ APEX
  #   /apex/com.android.btservices/lib64/.
  # Vendor-side fork libraries (libbthost_qti, libbluetooth_qti) match
  #   via separate vendor=qualcomm entries below — same CVE list, same
  #   semantics, separate attribution.
  # ===================================================================

  # F-FORENSIC-10 narrowing for AOSP-host Bluetooth entries: framework
  # code is chipset-agnostic; per-NVD scope is "Android Bluetooth host
  # stack" without enumerated chipsets. Minimum-impact category_regex
  # preserves exact-match semantics + satisfies the gate (Reviewer B B1
  # 2026-05-15). Future enhancement: per-Android-version version_regex
  # once vendor=aosp blobs surface a parseable version banner.

  - name: Bluedroid / Fluoride HID forced-pairing (CVE-2023-45866 / BleedingPodcast)
    vendor: aosp
    category: bluetooth
    category_regex: "^bluetooth$"
    cves:
      - CVE-2023-45866
    severity: high
    cvss_score: 7.1
    notes: |
      Marc Newlin disclosure 2023-12. BlueZ / Fluoride HID host accepts
      unauthenticated HID keystrokes from an attacker-controlled HID
      device that the host believes is already paired. RCE-adjacent
      (keystroke injection on locked screen). Fix shipped Android
      2023-12-05 SPL; classic_bonded_only=true enforced in pairing
      state machine. Devices below 2023-12 patch level: vulnerable.

  - name: Bluedroid / Fluoride GATT build_read_multi_rsp RCE (CVE-2023-40129)
    vendor: aosp
    category: bluetooth
    category_regex: "^bluetooth$"
    cves:
      - CVE-2023-40129
    severity: critical
    cvss_score: 8.8
    notes: |
      AOSP Bluetooth GATT heap overflow in build_read_multi_rsp().
      Remote code execution from attacker on adjacent BT controller
      with no privileges. Fixed in Android 2023-10-05 SPL (AOSP commit
      c0151aa3ba76c785b32c7f9d16c98febe53017b1). Affects Android 12.0,
      12.1, 13.0. Devices below 2023-10 patch level: vulnerable.

  - name: Bluedroid / Fluoride GATT integer-overflow precursor (CVE-2023-35673)
    vendor: aosp
    category: bluetooth
    category_regex: "^bluetooth$"
    cves:
      - CVE-2023-35673
    severity: high
    cvss_score: 7.8
    notes: |
      Integer overflow leading to heap overflow in build_read_multi_rsp.
      Predecessor finding to CVE-2023-40129; fixed in Android 2023-09-05
      SPL. Affects Android 11, 12, 12L, 13. Devices below 2023-09 patch
      level: vulnerable.

  - name: Bluedroid / Fluoride GATT DoS (CVE-2024-43763)
    vendor: aosp
    category: bluetooth
    category_regex: "^bluetooth$"
    cves:
      - CVE-2024-43763
    severity: medium
    cvss_score: 5.5
    notes: |
      Logic-error variant of the build_read_multi_rsp family; DoS
      rather than RCE. Fixed in Android 2024-10-05 SPL. Affects
      Android 13, 14.

  - name: Bluedroid / Fluoride OBEX cross-user disclosure (CVE-2024-49728)
    vendor: aosp
    category: bluetooth
    category_regex: "^bluetooth$"
    cves:
      - CVE-2024-49728
    severity: high
    cvss_score: 7.5
    notes: |
      OBEX file-sharing service permitted cross-user file disclosure.
      Fixed in Android 2024-12-05 SPL. Affects Android 12-15.
      Multi-user devices (tablets, automotive) most affected.

  # NOTE 2026-05-16 (Reviewer B M4): the previous
  # ADV-QTI-BT-HAL-2023 entry duplicating CVE-2023-35673 /
  # CVE-2023-40129 / CVE-2023-45866 under vendor=qualcomm has been
  # REMOVED. AOSP-side coverage (vendor=aosp entries above) already
  # fires on libbluetooth.so / bluetooth.default.so / libbluetooth_jni
  # via path_contexts; the libbt-vendor-qti.so / libbluetooth_qti.so
  # vendor-fork libraries get attribution through the SAME path-context
  # rule that classifies them as bluetooth/qualcomm. A separate
  # advisory was double-counting the CVEs without operational value
  # (vendor patch lag is real but not per-CVE actionable from static
  # analysis). If vendor-fork-specific divergence shipped, a future
  # entry would target a SPECIFIC vendor-patch-lag CVE (e.g. "QSB
  # 2024-03 omitted CVE-X fix") rather than re-emit AOSP IDs.

  # ===================================================================
  # wpa_supplicant CVE coverage — added 2026-05-16 per BTFM-correction
  # follow-up rec #4. Sources: NVD, Red Hat RHSA-2024:2517, Gentoo
  # GLSA-202309-16, Vanhoef Dragonblood research site.
  #
  # The wpa_supplicant binary lives under /vendor/bin/hw/ (modern
  # Android HAL service), /system/bin/ (legacy/GKI), or /usr/sbin/
  # (OpenWrt / generic Linux). vendor=aosp covers all three locations.
  # Per-CVE version threshold is documented in notes; operators verify
  # via `strings <binary> | grep '^wpa_supplicant v'`.
  # ===================================================================

  # F-FORENSIC-10 narrowing for AOSP wpa_supplicant entries: AOSP-host
  # framework code; per-NVD scope is wpa_supplicant version-banner
  # gated, not chipset gated. Minimum-impact category_regex preserves
  # exact-match semantics + satisfies the gate (Reviewer B B1 2026-05-15).
  # Future enhancement: per-version version_regex once the wpa_supplicant
  # version banner is parsed from binary strings.

  - name: wpa_supplicant PEAP-MSCHAPv2 phase-2 bypass (CVE-2023-52160)
    vendor: aosp
    category: wifi
    category_regex: "^wifi$"
    cves:
      - CVE-2023-52160
    severity: medium
    cvss_score: 6.5
    notes: |
      Top10VPN disclosure Feb 2024. wpa_supplicant <= 2.10 with PEAP
      enabled and TLS server certificate validation disabled accepts
      attacker-controlled phase-2 authentication, leaking the
      MSCHAPv2 challenge. Fixed in 2.11. Conditions: PEAP enabled in
      config AND ca_cert / ca_path NOT set. Android devices: ALL
      builds shipping wpa_supplicant <= 2.10 are CANDIDATES;
      exploitability requires the unsafe config.

  - name: wpa_supplicant / hostapd SAE side-channel (CVE-2022-23303)
    vendor: aosp
    category: wifi
    category_regex: "^wifi$"
    cves:
      - CVE-2022-23303
    severity: high
    cvss_score: 7.5
    notes: |
      hostapd + wpa_supplicant < 2.10 SAE implementation leaks password
      bits via cache access timing. Incomplete fix for CVE-2019-9494
      Dragonblood. Fixed in 2.10. Affects any WPA3-capable device with
      wpa_supplicant 2.x < 2.10.

  - name: wpa_supplicant / hostapd EAP-PWD side-channel (CVE-2022-23304)
    vendor: aosp
    category: wifi
    category_regex: "^wifi$"
    cves:
      - CVE-2022-23304
    severity: high
    cvss_score: 7.5
    notes: |
      Companion to CVE-2022-23303 — EAP-PWD implementation leaks
      password bits via cache timing. Fixed in 2.10.

  - name: wpa_supplicant Dragonblood SAE / EAP-PWD cluster
    # Advisory_id ≤20 chars per Rule #15 / VARCHAR(20) cve_id column.
    # Reviewer A L5 (2026-05-16): shortened from ADVISORY-WPAS-DRAGONBLOOD
    # (25 chars) so the schema validator doesn't warn + matches the
    # at-use clip behaviour explicitly.
    advisory_id: ADV-WPAS-DRAGONBLD
    vendor: aosp
    category: wifi
    category_regex: "^wifi$"
    cves:
      - CVE-2019-9494
      - CVE-2019-9495
      - CVE-2019-9496
      - CVE-2019-13377
    severity: high
    cvss_score: 7.5
    notes: |
      Vanhoef + Ronen 2019. WPA3 SAE handshake design flaws + EAP-PWD
      timing side-channels. CVE-2019-9494 (SAE timing), -9495 (EAP-pwd
      cache side-channel), -9496 (invalid-curve ECC), -13377 (SAE
      Brainpool timing). All four fixed by wpa_supplicant 2.9.
      Affects any pre-2.9 build.

  # ===================================================================
  # NVIDIA Tegra / L4T BSP CVE cluster — added 2026-05-15 per
  # postmortem hw-firmware-adaptive-session-2026-05-18 Rec #3
  # (Reviewer A C2 + Reviewer B F-FORENSIC-03 deferred). Each pin
  # verified against NVD CPE list via direct WebFetch on
  # services.nvd.nist.gov/rest/json/cves/2.0?cveId=<CVE>. Recursive-
  # verification discipline (Reviewer B 2026-05-15..18) applied:
  # Scout report's per-CVE CPE claims independently re-fetched
  # before commit.
  #
  # 3 of 6 had user-prompt-discrepancies that NVD overruled:
  #   * CVE-2021-1111  — user said "ALL Jetsons"; NVD CPE EXCLUDES
  #                      TX1/Nano (only AGX-Xavier+TX2+TX2-NX+Xavier-NX).
  #   * CVE-2021-34372 — user said product=tegra_tos_trusty; NVD CPE
  #                      uses product=jetson_linux. wairz curated
  #                      schema has no `product` field; we narrow via
  #                      vendor=nvidia + category=tee instead.
  #   * CVE-2022-42269 — user said "AGX-Xavier/TX2/Xavier-family";
  #                      NVD CPE ALSO includes jetson_tx1 (T210).
  #
  # NVD wins per Rule #19 evidence-first in all 3 cases.
  #
  # FORWARD-PREPARED NOTE: version_regex requires L4T BSP release
  # string ("R32.3.1" etc.) to appear in blob.version OR any blob
  # metadata value. The current Tegra parser (parsers/tegra_blob.py,
  # shipped commit 8054d22 2026-05-15) does NOT yet extract L4T
  # release from blob content. These pins will fire when a future
  # enhancement adds L4T release extraction (e.g. parse
  # /etc/nv_tegra_release on the firmware tree → populate
  # firmware.device_metadata["l4t_release"], OR scan Tegra-blob
  # head for "R<N> (release), REVISION: <x.y>" banner string and
  # store in blob.metadata). Until then the pins are documented
  # forward-prepared entries — strict version_regex prevents
  # over-attribution (Reviewer B 2026-05-18 discipline).
  #
  # OUT OF SCOPE this session per user direction: CVE-2021-34373..
  # 34396 from the same NVIDIA security bulletin disclosure batch.
  # The per-NVD-CPE recipe applies recursively for any future session
  # extending Tegra coverage — DO NOT batch-extrapolate. Each CVE
  # gets its own NVD WebFetch verification before pinning.
  #
  # Tegra chipset codes: T210 = TX1/Nano (Maxwell), T186 = TX2
  # (Pascal), T194 = Xavier (Volta), T234 = Orin (Ampere; not
  # affected by any of these 6 CVEs per NVD CPE).
  # ===================================================================

  - name: Tegra TX1 nvtboot Selfblow (CVE-2019-5680)
    vendor: nvidia
    category: bootloader
    chipset_regex: "(?i)^(t210|tegra210|tx1)$"
    version_regex: "(?i)(r3[01]\\b|r32\\.[01]\\b|r32\\.0\\.|r32\\.1\\.)"
    strict_chipset: false
    cves:
      - CVE-2019-5680
    severity: high
    cvss_score: 6.7
    notes: |
      nvtboot load-address validation bypass on Tegra TX1 (T210).
      NVD CPE: jetson_tx1_firmware versionEndExcluding=r32.2.
      TX1-ONLY — explicitly EXCLUDES TX2 / Xavier / Nano.
      Fix landed in L4T R32.2 (June 2019); R32.2 IS the fix cohort
      (Reviewer B 2026-05-18 caught earlier R32.3.1 mis-attribution).
      Reference: https://nvd.nist.gov/vuln/detail/CVE-2019-5680

  - name: Tegra NV3P USB recovery bounds-check (CVE-2021-1111)
    vendor: nvidia
    category: bootloader
    chipset_regex: "(?i)^(t186|t194|tegra186|tegra194|tx2|tx2-nx|tx2nx|xavier-nx|xaviernx|agx-xavier|agxxavier)$"
    version_regex: "(?i)(r32\\.[1-5]\\b|r32\\.6\\.0\\b|r32\\.[1-5]\\.|r32\\.6\\.0\\.)"
    strict_chipset: false
    cves:
      - CVE-2021-1111
    severity: medium
    cvss_score: 6.7
    notes: |
      NV3P recovery-mode bounds-check failure (USB physical access).
      NVD CPE: jetson_linux versionStartIncluding=32.1 AND
      versionEndExcluding=32.6.1. Hardware = AGX-Xavier + TX2 +
      TX2-NX + Xavier-NX ONLY. TX1 + Nano + Nano-2GB EXPLICITLY
      EXCLUDED per NVD (contradicts session-prompt's "ALL Jetsons"
      claim — NVD wins per Rule #19 evidence-first).
      Fix in L4T R32.6.1 (Aug 2021).
      CVSS 3.1 Base Score 6.7 MEDIUM per NVIDIA CNA (NVD primary);
      legacy CVSS 2.0 = 4.6 MEDIUM (NIST secondary).
      Reference: https://nvd.nist.gov/vuln/detail/CVE-2021-1111

  - name: Tegra Trusty OTE integer overflow (CVE-2021-34372)
    vendor: nvidia
    category: tee
    version_regex: "(?i)(r3[01]\\b|r32\\.[0-4]\\b|r32\\.5\\.0\\b|r3[01]\\.|r32\\.[0-4]\\.|r32\\.5\\.0\\.)"
    strict_chipset: false
    cves:
      - CVE-2021-34372
    severity: high
    cvss_score: 7.8
    notes: |
      Integer overflow in malloc() size calc in Trusty OS OTE
      protocol parser. NVD CPE: jetson_linux versionEndExcluding=
      32.5.1. Hardware list = ALL Jetsons (TX1 + TX2 family + AGX-
      Xavier + Xavier-NX + Nano + Nano-2GB) — the broadest scope of
      the 6 Tegra CVEs.
      Session prompt claimed product=tegra_tos_trusty; NVD CPE uses
      product=jetson_linux (NVD wins per Rule #19). wairz curated
      schema has no `product` field; we narrow via vendor=nvidia +
      category=tee instead. Fix in L4T R32.5.1.
      Reference: https://nvd.nist.gov/vuln/detail/CVE-2021-34372

  - name: Tegra MB2 free-the-wrong-heap (CVE-2021-34397)
    vendor: nvidia
    category: bootloader
    chipset_regex: "(?i)^(t186|t194|tegra186|tegra194|tx2|tx2-4gb|tx2-nx|tx2nx|tx2i|xavier-nx|xaviernx|agx-xavier|agxxavier)$"
    version_regex: "(?i)(r3[01]\\b|r32\\.[0-4]\\b|r32\\.5\\.0\\b|r3[01]\\.|r32\\.[0-4]\\.|r32\\.5\\.0\\.)"
    strict_chipset: false
    cves:
      - CVE-2021-34397
    severity: low
    cvss_score: 2.3
    notes: |
      MB2 bootloader free-the-wrong-heap → limited DoS. NVD CPE:
      jetson_linux versionEndExcluding=32.5.1. Hardware anchors =
      AGX-Xavier (8/16/32GB) + TX2 (incl. 4GB/i/NX) + Xavier-NX
      (dev/prod). TX1 + Nano EXPLICITLY EXCLUDED per NVD.
      Fix in L4T R32.5.1.
      CVSS 3.1 Base Score 2.3 LOW per NIST (NVD primary; AV:L/AC:L/
      PR:H/UI:N/S:U/C:N/I:N/A:L); NVIDIA CNA secondary = 1.9 LOW.
      Limited DoS only — requires local high-privilege access; no
      info disclosure or integrity impact. Reviewer B 2026-05-15
      caught initial pin shipped with overrated severity=medium /
      cvss=5.5 (the per-CVE NVD primary check is the authoritative
      source for both attribution AND severity grading per Rule #19).
      Reference: https://nvd.nist.gov/vuln/detail/CVE-2021-34397

  - name: Tegra Trusty SMC input validation (CVE-2022-42269)
    vendor: nvidia
    category: tee
    chipset_regex: "(?i)^(t210|t186|t194|tegra210|tegra186|tegra194|tx1|tx2|tx2-4gb|tx2-nx|tx2nx|tx2i|xavier-nx|xaviernx|xavier-nx-16gb|agx-xavier|agxxavier|agx-xavier-8gb|agx-xavier-16gb|agx-xavier-32gb|agx-xavier-64gb|agx-xavier-industrial)$"
    version_regex: "(?i)(r3[01]\\b|r32\\.[0-6]\\b|r32\\.7\\.[01]\\b|r3[01]\\.|r32\\.[0-6]\\.|r32\\.7\\.[01]\\.)"
    strict_chipset: false
    cves:
      - CVE-2022-42269
    severity: high
    cvss_score: 7.9
    notes: |
      Trusty OS SMC handler missing input validation → info disclosure
      + integrity compromise. NVD CPE: jetson_linux versionEnd
      Excluding=32.7.2. Hardware anchors INCLUDE jetson_tx1 in
      addition to AGX-Xavier (6 SKUs) + TX2 (4 SKUs incl. TX2-NX) +
      Xavier-NX (2 SKUs) — contradicts session-prompt's
      "AGX-Xavier/TX2/Xavier-family" scope which omitted TX1.
      NVD wins per Rule #19 evidence-first; TX1 added to
      chipset_regex. Jetson Nano (also T210 but a different
      hardware anchor) is NOT in the NVD CPE list and is excluded.
      Fix in L4T R32.7.2 (Aug 2022).
      Reference: https://nvd.nist.gov/vuln/detail/CVE-2022-42269

  - name: NVDLA emu_task_submit Xavier-only (CVE-2022-42270)
    vendor: nvidia
    category: kernel_module
    chipset_regex: "(?i)^(t194|tegra194|xavier-nx|xaviernx|xavier-nx-16gb|agx-xavier|agxxavier|agx-xavier-8gb|agx-xavier-16gb|agx-xavier-32gb|agx-xavier-64gb|agx-xavier-industrial)$"
    version_regex: "(?i)(r3[01]\\b|r32\\.[0-6]\\b|r32\\.7\\.[01]\\b|r3[01]\\.|r32\\.[0-6]\\.|r32\\.7\\.[01]\\.)"
    strict_chipset: false
    cves:
      - CVE-2022-42270
    severity: high
    cvss_score: 7.8
    notes: |
      NVDLA emu_task_submit stack-based buffer overflow. NVD CPE:
      jetson_linux versionEndExcluding=32.7.2. Xavier-ONLY: NVDLA
      Deep Learning Accelerator is only on T194/Xavier silicon.
      Hardware anchors = AGX-Xavier (6 SKUs) + Xavier-NX (2 SKUs).
      TX1 / TX2 / Nano EXPLICITLY EXCLUDED per NVD.
      category=kernel_module because NVDLA driver lives in
      nvidia.ko / nvgpu kernel module.
      Fix in L4T R32.7.2.
      Reference: https://nvd.nist.gov/vuln/detail/CVE-2022-42270
