# HANDOFF — cra-exploitability-deep-review (2026-06-04→06-05 arc)

**Generated:** 2026-06-08 · **Campaign:** cra-exploitability-deep-review-2026-05-30 (status: active)
**Repos:** wairz (session root; `.planning/` lives here) · cve-assessment-framework (nested, separate .git — all 63 commits this arc; a private Bitbucket workspace, branch `master`, pushed, 0 ahead)
**Knowledge:** `.planning/knowledge/cra-exploit-deep-review-rootcause-teamshare-2026-06-05-{patterns,antipatterns}.md`
**Postmortem:** `.planning/postmortems/postmortem-cra-exploit-deep-review-2026-06-05.md`

## What was built / settled this arc
- **Canonical state v1.37.0** (`output/eu-cra-conformance-package/canonical-state.json`): **277 initial-entry heads** (DEVICE_A 79 / phone 69 / tablet 129), exploitable 23,027, chainable 22,750, not_reachable 1,560, undetermined 20,453, CRA **NON-COMPLIANT** / RED **NON-CONFORMS**. Heads moved 364→368 (RC-1 re-opens) →277 (adversarial deep-dive demoted 91 with positive proof).
- **VEX** (authoritative): `output/{device_a,phone,tablet}_vex.json` at v107/v88/v86; affected 4,739/11,511/6,777. All 6 representations (OpenVEX + CycloneDX, canonical + 3 package mirrors) regenerated to match.
- **RC-1 mis-attribution gate**: `scripts/audit_misattribution.py` (resolved-aware, exempts `MISATTR-FP`). New apply scripts: `apply_cvestudio_corrections.py`, `apply_review_vex_corrections.py`, `apply_misattr_verdicts.py`, `apply_heads_deepdive_verdicts.py`.
- **Gates (all GREEN):** CR-1 over-clear (`audit_vex_overclear.py`), kill-chain completeness, residual-token, doc-drift **+ malformed-span guard** (`refresh_package_docs.py --check`), stale-number (`audit_doc_numbers.py`), mis-attribution.
- **Conformance package** converged data-only: master report + heads report + DEVICE_A kill-chain + critique + Excel + educational + CTL memo, all span-wired. 9 superseded docs in `_archive/`.
- **Team-share package** (the deliverable): `output/DEVICE_A-Security-Conformance-Team-Package.zip` (8MB) — 20 curated docs in a 6-section mkdocs nav + pre-built `_site/` + the Excel + README. Source tree at `output/team-share/`. Reviewed clean (zero over-share, zero refs to excluded docs).

## Key decisions (don't relitigate)
- One workflow at a time, BATCH=4-5 (429-cascade lesson). · Span-wire all current-state numbers. · Re-open weak `not_reachable` to chainable, never launder (Axiom 1). · Archive (don't delete) superseded docs. · Clean-ups-only to originals; keep audit-legitimate content (vendor-chains, methodology, audit-trail). · Recursive review→fix→re-verify until count→0.

## How to resume / regenerate
- After any VEX/disposition change: `generate_canonical_state.py --apply --package-version X` → `generate_head_report.py` → `refresh_package_docs.py` → run all 6 gates → for VEX, `sync_cyclonedx_vex_from_openvex.py` + copy mirrors.
- Rebuild team-share: `scripts/build_team_share.py` → `mkdocs build` → re-zip. (Workflow scripts: `output/_run_*.js`; plans: `output/_*_plan.json`.)
- Use `uv run python3` for framework scripts (need 3.11+ datetime.UTC).

## Unresolved / carry-forward
1. **Phone/tablet exploitability held GUILTY** pending a live ADB lockdown capture (`dumpsys device_policy` + cellular state) — the largest unmeasured reachability switch (Axiom 1). Would collapse phone heads (69) toward ~27 under confirmed kiosk+no-SIM.
2. **Mis-attribution audit is device_a-only** (Linux package manifest); phone/tablet (sparse Android manifests) need a SoC-CPE/Android-version variant to cover the same false-clear class.
3. **Internal-only docs** (METHODOLOGY, FRAMEWORK-*, ADDENDUM) still legitimately name the tooling — a full tool-neutral sweep of the *internal* package is a deferred, optional follow-up (NOT needed for the team-share, which is clean).
4. Constraints persist: NO Co-Authored-By trailers; do not re-run `batch_triage.py` wholesale; external-action consent-marker dance before any push.
