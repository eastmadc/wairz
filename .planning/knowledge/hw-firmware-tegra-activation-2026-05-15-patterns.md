# Patterns: HW-Firmware Tegra CVE Pin Activation + F-FORENSIC-10 Schema Gate + state_snapshot — 2026-05-15

> Extracted: 2026-05-15 (afternoon)
> Campaign: ad-hoc Citadel-driven session shipping 3 carried-forward recommendations from postmortem `postmortem-hw-firmware-mcp-tegra-2026-05-15.md`
> Postmortem: `.planning/postmortems/postmortem-hw-firmware-tegra-activation-2026-05-15.md`

## Successful Patterns

### 1. Recursive NVD-CPE verification extends to chipset_regex ENUMERATED VALUES (6 sessions running)
- **Description:** The morning session established that NVD-CPE verification applies to user prompts + scout reports + reviewer findings + CVSS field values. This session EXTENDED the discipline to chipset_regex ENUMERATED VALUES — every value in the regex needs NVD-CPE provenance, not just the regex's general SCOPE. Reviewer B caught CVE-2017-18159 chipset_regex over-broad inclusion of post-2018 Snapdragon codenames (bengal/kona/lahaina/waipio/niobe/sun/magpie/moorea/saipan) that POST-DATE the 2018-06-05 CAF patch — these would over-attribute on 2020+ XBL blobs that NVD does NOT list as affected.
- **Evidence:** 8 independent NVD WebFetches this session (1 implementer-side + 7 Reviewer-B-side); 1 CRITICAL chipset_regex drift caught + fixed via tightening to NVD-faithful 2018-cohort prefixes only `(?i)^(msm|mdm|qm[0-9]|sdm[0-9]{3}|sd[0-9]{3}|sda[0-9]{3})`.
- **Applies when:** Any session pinning a CVE attribution with a chipset_regex narrowing. The regex CONTENTS (not just the regex shape) need per-value NVD-CPE provenance. **Cross-reference:** CVE-2019-5680 (R32.3.1+ misattribution caught morning session), CVE-2017-18159 (chipset_regex contents over-broad caught this session) — both follow the same recursive-discipline-failure pattern.

### 2. Pre-implementation width-canary audit (Rule #31) prevents migration scope explosions
- **Description:** When activating a destructive change (like a schema gate that SKIPS non-conforming entries), audit the entire migration scope BEFORE landing the change. A single SQL `COUNT(*)` (~1 second) reveals whether the user-prompt's named entries are the full scope or just one instance of a broader pattern. The F-FORENSIC-10 gate's user prompt named only CVE-2017-18159 needing pre-narrowing; the audit revealed 16 entries (15 additional) at risk — would have silently dropped ~5,000 CVE rows on next cve-match.
- **Evidence:** Pre-implementation `SELECT cve_id, COUNT(*) FROM sbom_vulnerabilities WHERE cve_id IN (...) GROUP BY cve_id` revealed 22 of 25 audited CVEs firing with 5,000+ blob rows. Pre-narrowed all 16 entries before gate activation. Post-implementation regression check: BRAKTOOTH baseline 54 rows + CVE-2017-18159 147 rows + 0 wrong-attribution — all preserved.
- **Applies when:** Activating any destructive schema change. The user prompt's named scope is a CANDIDATE; the database truth is the full scope. Pre-implementation audit costs ~1 second and eliminates regression risk.

### 3. Forward-prepared pin activation via N follow-up commits — incremental gap-closure
- **Description:** Forward-prepared CVE pins shipped previous session (commit 6bc1c1d) as "waiting for L4T release extraction". This session activated them via 3 commits: (a) content-banner scan, (b) path-inference fallback, (c) raw_bin 5th magic-byte-bypass fallback. Each fixup driven by end-to-end corpus verification surfacing a gap the prior commit didn't close. Unit tests passed at each stage; only real-corpus verification revealed the corpus-shape fit issues.
- **Evidence:** Initial TARGET 1 (`f54d415`) shipped + 28 tests passed → 0 DEVICE_A blobs received l4t_release. Fixup #1 (`0a901f2`) path inference added → 39 DTBs received l4t_release but cve-match returned 0 (wrong category). Fixup #2 (`338f95b`) 5th fallback for raw_bin → 63 blobs received l4t_release including 11 bootloaders + 4 TEE → cve-match returned 30 new rows on 4 of 6 pins.
- **Applies when:** Activating any forward-prepared feature where the canonical implementation may not cover real corpus shape. **Per Rule #19 evidence-first** — incremental real-corpus verification IS the test that matters; unit tests catch the function shape but not the corpus-shape fit. Budget for N follow-up commits per Rule #25 rather than trying to land the perfect implementation in one shot.

### 4. Multi-stage fallback chain (content-first → path-inference → magic-bypass-with-context-gate)
- **Description:** When a primary extraction path (e.g. blob-content scan for a banner string) doesn't cover the entire real corpus shape, layer fallbacks in order of evidence quality: (1) content scan (most authoritative — Rule #19 evidence-first), (2) path inference (cheap fallback when content is unavailable), (3) magic-bypass with context-gate (covers deferred-TBD subsets). Each layer has its own positive + negative tests. Precedence is content > path (content-banner-wins-over-path-inference Rule #19 canary asserts this).
- **Evidence:** Commits `f54d415` + `0a901f2` + `338f95b` implement the 3-stage chain. 35 paired-canary tests verify each stage's positive + negative cases + the precedence rule. End-to-end DEVICE_A result: 63 of 99 blobs receive l4t_release (vs 0 without the chain).
- **Applies when:** Designing extraction for a field that may live in multiple forms (inline content, path encoding, filename pattern, sibling-file presence). Layer fallbacks; gate aggressive fallbacks with context checks to prevent false-positives.

### 5. Per-component path-context gate prevents whole-path-substring false-positives
- **Description:** When using path substring matching for heuristic attribution, scope the check to the path SEGMENT containing the signal — not the whole path. Whole-path substring matches surface false-positives when an UPSTREAM path component contains the gate token unrelated to the signal-bearing component. pytest tmp paths like `/tmp/pytest/test_l4t_release/MyProject_R32.3.1_backup/` contain "l4t" in the test name far from the version-bearing component — whole-path match returned True; per-component match returns False.
- **Evidence:** `_has_tegra_path_context()` initially used whole-path substring; failed pytest. Switched to per-component check (only the path component containing R<N>.<x>.<y> match counts) — pytest tmp paths handled correctly.
- **Applies when:** Any path-based heuristic gate. The path is a multi-segment structure; scope checks to the relevant segment. Generalises Rule #46 (paired-canary) — the per-component check IS the canary's positive shape; whole-path matching would have silently broken under realistic file system layouts.

### 6. state_snapshot() public method as abstraction-boundary fix; defensive getattr was the mask
- **Description:** Replacing private-attr access via `getattr(loader, "_field", None)` with a public state_snapshot() method removes the silent-masking behavior. The defensive shape returned None on missing attrs without exception — any future rename of `_cached_mtime_ns` → `_atomic_mtime` would have silently produced `yaml_mtime_iso=null/status=defaults` with no exception. The public method forces an AttributeError on any class-shape change — refactor signals now surface as load-bearing errors.
- **Evidence:** Commit `bbaef3b` shipped 8-key stable contract dict from `state_snapshot()` + 9 new tests including Rule #46 META-CANARY asserting the public method exists on the loader (`test_surface_state_payload_no_private_attr_access_required`). Grep confirms zero external `_cached_mtime_ns` / `_loaded_from_yaml` accesses outside yaml_cache.py post-refactor.
- **Applies when:** Any abstraction-boundary violation where a consumer reads private attrs of a class. Add a public state-snapshot method; remove the defensive getattr; ship a Rule #46 META-CANARY asserting the contract.

### 7. Cross-domain helper placement signals scope (Reviewer A A7 extension)
- **Description:** `surface_state_payload` helper moved from `tools/hardware_firmware.py` (hw-firmware-specific) to `app/utils/yaml_cache.py` (cross-domain). The helper is generic over `MtimeCachedYamlLoader[Any]` — placement under `app/utils/` invites future consumers from any domain that adopts the YAML cache primitive. Reviewer C scout identified 2-3 candidate sites (taint_llm YAML loaders) that should adopt the same hot-reload + state-snapshot shape.
- **Evidence:** Commit `bbaef3b` moved the helper + updated import in `tools/hardware_firmware.py`. Existing tests in `test_hardware_firmware_mcp_tools.py` continue to pass (same JSON output shape). Scout C identified `app/ai/tools/taint_llm.py:78-101` as a cold-cache YAML adoption candidate.
- **Applies when:** Authoring or moving a helper that's generic over a primitive (`MtimeCachedYamlLoader[Any]`, `ParsedBlob`, `Settings`, etc.). Module location signals scope per the morning's Pattern #9 / Reviewer A A7 — placement under `app/utils/` invites cross-domain adoption.

### 8. Minimum-impact ceremonial narrowing satisfies a schema gate without behavior change
- **Description:** For YAML entries whose NVD-CPE scope doesn't enumerate further than vendor+category (framework-level CVEs like Bluedroid, binary-level CVEs like wpa_supplicant), satisfy the F-FORENSIC-10 gate via `category_regex: ^<existing_category>$`. Preserves exact-match semantics (the matcher uses case-insensitive `re.search` — anchored regex matches the existing exact-match value). Satisfies the gate's narrowing-field-presence check. Documents the deferred work via inline YAML comments referencing the genuine NVD-derived narrowing follow-up.
- **Evidence:** Commit `5398f16` applied this pattern to 14 of 16 pre-narrowed entries. Regression check: all 14 entries preserved their currently-firing rows (FragAttacks 1176 × 4 firmwares, MediaTek modem 20 × 3 firmwares, wpa_supplicant 36-38 × 3-4 firmwares, etc.).
- **Applies when:** A schema gate requires "narrowing field set" but the entry's NVD scope is genuinely at the vendor+category level (no further enumeration). Ceremonial narrowing satisfies the gate + preserves behavior + documents the deferred genuine-narrowing work. **Anti-pattern to avoid:** treating ceremonial narrowing as a cargo-cult substitute for genuine NVD-derived narrowing on entries where the latter IS available.

### 9. on_conflict_do_nothing detector behavior — existing rows aren't updated on re-detect
- **Description:** The detector uses `stmt.on_conflict_do_nothing(constraint="uq_hwfw_firmware_sha256")` — when re-detecting an existing firmware, blob rows whose `(firmware_id, blob_sha256)` already exist are SKIPPED. For verifying that a parser-output change (new metadata field) takes effect on already-detected firmware, DELETE the existing blob rows FIRST + re-run detection. Documented Pattern #9 from this session — saves debugging time when "did this code change affect output?" questions arise.
- **Evidence:** Caught mid-session: initial re-detect after L4T extraction landing returned "99 blobs detected" but SQL showed 0 blobs with `metadata->>'l4t_release'`. `on_conflict_do_nothing` was the cause. `DELETE FROM hardware_firmware_blobs WHERE firmware_id = '...'` + re-detect → 63 blobs with l4t_release populated.
- **Applies when:** Verifying any parser-output / classifier-output change on previously-detected firmware. Document the DELETE-then-redetect step in test plans; consider whether parser-output backfill needs a one-shot SQL UPDATE for the existing corpus OR a deliberate detector behavior change.

### 10. Pre-implementation parallel scouts + implementer-side independent verification (continued pattern)
- **Description:** 3 parallel Citadel scouts dispatched in one batch — one per TARGET (1/2/3). Each returned a complete spec under 500 words within ~5 minutes. PLUS the implementer side ran 1 independent NVD WebFetch on CVE-2017-18159 to verify the chipset_regex scope. Reviewer-side dispatch then ran 7 MORE NVD fetches — independent verification at a third layer. The implementer's verification caught some drifts; the reviewer's caught the CRITICAL post-2018 codename inclusion the implementer missed.
- **Evidence:** Pre-implementation 3 Agent dispatches ran in parallel (~5 min wall-clock); 1 implementer-side WebFetch + 7 reviewer-side WebFetches = 8 independent NVD verifications total. Caught 1 CRITICAL drift the implementer-side missed.
- **Applies when:** Any session where multiple TARGETs can be researched in parallel + the implementer + reviewer can independently verify against the same authoritative source. **Triple-verification (scout → implementer → reviewer) catches drift that single-axis verification misses** — different humans focus on different aspects; the implementer focuses on regex shape while the reviewer focuses on regex CONTENTS.

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| L4T release stored at TOP-LEVEL `metadata["l4t_release"]` (NOT nested under `tegra_blob`) | Reviewer B B4 from morning session — `_stringify_metadata` walks one level deep ONLY. Nested storage would silently defeat the version_regex match path. | Worked. End-to-end DEVICE_A cve-match found L4T release via top-level metadata. |
| Two-stage extraction (content-banner first; path-inference fallback) | Content-banner is more authoritative per Rule #19 evidence-first (banner inside the blob is closer to the truth than archive directory name). Path-inference is the cheap fallback when content isn't available. Precedence canary asserts content-wins. | Worked. 4 DEVICE_A blobs with content banner (likely tegraflash subset); rest via path inference. |
| 5th magic-byte-bypass fallback with per-component context gate | Raw_bin Tegra blobs (NVIDIA wrapper TBD subset) wouldn't fire any of the 4 subset parsers, so path-inference never ran. 5th fallback covers them; per-component gate prevents pytest tmp path false-positives. | Worked. 24 raw_bin Tegra blobs (11 bootloaders + 4 TEE + 6 MCU + 1 DSP + 1 USB + 1 camera) received l4t_release via 5th fallback. |
| F-FORENSIC-10 gate as SKIP-reject (not WARN-only) | Mirrors the BT YAML layer's `_parse_banner_cve_pin` shape (commit 51db3c8 2026-05-18). WARN-only would be soft advice; the disclosure-batch antipattern needs hard prevention. Pre-narrowing 16 entries before activation eliminates regression risk. | Worked. 0 currently-loaded entries rejected by gate; all 5,000+ CVE rows preserved. Future authors get loud WARN-skip on family-only entries. |
| Pre-narrow CVE-2017-18159 with chipset_regex (per user prompt) AND 14 others with category_regex | Per Rule #31 width-canary: audit revealed 15 additional entries at risk. Genuine NVD-derived narrowing for CVE-2017-18159 (per user prompt scope); minimum-impact ceremonial narrowing for the rest preserves behavior + satisfies gate + documents deferred genuine-narrowing work. | Worked. All 16 entries pass gate post-narrowing; deferred work documented in inline YAML comments + postmortem. |
| Reviewer B chipset_regex fixup shipped as separate fixup commit (Rule #25) | The fixup is independently revertable from the original gate landing. Bisect-clean — if a future regression surfaces, the fixup's effect can be isolated. | Worked. Per Rule #25 per-piece commits, the Reviewer B catch is its own commit (e93920e) bisect-clean against the F-FORENSIC-10 gate commit (5398f16). |
| Activation-gap fixups (path-inference + 5th fallback) shipped as separate commits | Each fixup driven by end-to-end verification surfacing a different gap. Per Rule #25, each is independently revertable. The chain (commit f54d415 → 0a901f2 → 338f95b) represents incremental gap closure rather than a single perfect implementation. | Worked. The 2 fixups are bisect-clean against each other + against the original TARGET 1 landing. |

## Patterns Cross-Referenced Across Sessions

This session's patterns extend prior:

- **Morning session Pattern #1** (Recursive NVD-CPE verification extends to user-prompt claims + CVSS field values) → **extended to chipset_regex ENUMERATED VALUES**. The regex CONTENTS need per-value NVD provenance, not just the regex's general SCOPE. Reviewer B caught the post-2018 Snapdragon codename inclusion.
- **Morning session Pattern #4** (Content-evidence parser end-to-end validation on real operator-modified corpus) → reinforced. The DEVICE_A corpus shape (raw_bin Tegra wrappers, no inline banner) drove 2 activation-gap fixups that unit tests wouldn't have caught. Real-corpus verification IS the test that matters.
- **Morning session Pattern #5** (Single MCP tool closing multiple Reviewer findings) → state_snapshot + surface_state_payload close Reviewer A A6+A7 in one refactor commit (`bbaef3b`).
- **Morning session Pattern #6** (last_warning operator-visibility primitive) → carries forward unchanged; state_snapshot now surfaces it via the public method.
- **Morning session Pattern #7** (Cross-stack alignment tests cheaper than runtime-derivation) → applied to the gate analog at curated tier vs BT layer. Reviewer A flagged the cross-stack alignment test as missing — deferred.
- **Morning session Pattern #8** (Forward-prepared CVE pins ship with documented activation conditions) → ACTIVATED this session via 3 commits. The pattern's promise ("activate when L4T-release-extraction commit lands") delivered on; 4 of 6 pins fired per NVD-scope-correct activation.
- **Morning session Pattern #9** (Detection persistence requires explicit commit from standalone async contexts) → reinforced. Standalone redetect script + cve-match script both used explicit `await db.commit()` per Pattern #9.
- **Rule #25 per-piece commits** — applied at 6-commit chain (3 feat + 1 fix + 2 activation-gap feat) all bisect-clean. The 2 activation-gap fixups demonstrate that "perfect implementation in one commit" is sometimes the wrong shape — incremental gap closure via fixup commits is cleaner.
- **Rule #46 paired-canary discipline** → applied to 25 new tests across this session. Every "asserts ABSENCE" gate has a synthesized-violation canary. The state_snapshot META-CANARY asserts the abstraction-boundary contract (`test_surface_state_payload_no_private_attr_access_required`) — a new shape of canary asserting CLASS-SHAPE invariants not just runtime behavior.
- **Rule #31 width-canary discipline** → applied to the F-FORENSIC-10 gate's migration scope BEFORE activation. SQL audit revealed 16 entries at risk; pre-narrowed all before landing the gate. Generalised pre-implementation discipline for ANY destructive schema change.
- **Rule #19 evidence-first** → extended to chipset_regex contents AND to multi-stage fallback precedence (content-banner > path-inference). The corpus is the truth; the parser must fit the corpus shape, not the canonical-case shape.
- **6 sessions running with multi-persona reviewer pattern** — 2026-05-15 morning (CVE-2021-1111 ALL-Jetsons + CVSS drifts), 2026-05-16, 2026-05-17, 2026-05-18, 2026-05-15 PM (this session — CVE-2017-18159 chipset_regex over-broad). Discipline durable beyond debate.
